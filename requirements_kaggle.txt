# Packages NOT pre-installed on Kaggle's nvidia-pytorch GPU image.
# NOTE: torch / torchvision / numpy / pandas / pycocotools / tqdm ARE present
# (with CUDA). segmentation-models-pytorch and timm are installed by the
# kernel script WITH --no-deps so pip does NOT replace the CUDA torch with a
# CPU-only build. Only the lightweight leaf deps below are listed here.
albumentations>=1.3
scikit-image>=0.21
scipy>=1.10
opencv-python-headless>=4.7
pyyaml>=6.0
einops
pretrainedmodels
huggingface-hub
