# Only packages NOT pre-installed on Kaggle's nvidia-pytorch GPU image.
# torch / torchvision / numpy / pandas / pycocotools / tqdm are already present,
# so we avoid reinstalling them (which would be slow and could break CUDA).
albumentations>=1.3
scikit-image>=0.21
scipy>=1.10
opencv-python-headless>=4.7
segmentation-models-pytorch>=0.3.3
timm>=0.9
pyyaml>=6.0
